[project]
name = "sanka-migrate-connector-hubspot"
version = "0.1.0.dev0"
description = "Sanka Migrate connector: migrate CRM data out of and into HubSpot"
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{ name = "Sanka, Inc." }]
dependencies = [
  "sanka-migrate-connector-sdk",
  "httpx>=0.27",
]

[project.entry-points."sanka.connectors"]
hubspot = "sanka_connector_hubspot:CONNECTOR"

[project.urls]
Repository = "https://github.com/sankaHQ/sanka-migrate"
Issues = "https://github.com/sankaHQ/sanka-migrate/issues"

[tool.uv.sources]
sanka-migrate-connector-sdk = { workspace = true }

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sanka_connector_hubspot"]

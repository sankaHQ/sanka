[project]
name = "sanka-migrate-connector-csv"
version = "0.1.0.dev0"
description = "Sanka Migrate connector: read a CSV or TSV file as a migration source"
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{ name = "Sanka, Inc." }]
dependencies = [
  "sanka-migrate-connector-sdk",
]

[project.entry-points."sanka.connectors"]
csv = "sanka_connector_csv:CONNECTOR"

[project.urls]
Repository = "https://github.com/sankaHQ/sanka"
Issues = "https://github.com/sankaHQ/sanka/issues"

[tool.uv.sources]
sanka-migrate-connector-sdk = { workspace = true }

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sanka_connector_csv"]

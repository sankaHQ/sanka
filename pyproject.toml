# Workspace root — not a published package. Shared tooling config for all packages.
[project]
name = "sanka-migrate-workspace"
version = "0.0.0"
description = "Development workspace for the Sanka open-source monorepo"
requires-python = ">=3.12"

[tool.uv]
package = false

[tool.uv.workspace]
members = [
  "packages/sanka-migrate",
  "packages/sanka-migrate-mcp",
]

[dependency-groups]
dev = [
  "ruff>=0.12",
  "mypy>=1.14",
  "pytest>=8.3",
  "pytest-asyncio>=0.24",
  "django>=5.2,<7",
  "djangorestframework>=3.16,<4",
  "fastapi>=0.115,<1",
  "httpx>=0.27,<1",
  "sanka-connector-clickhouse==0.1.0a9",
  "sanka-connector-csv==0.1.0a9",
  "sanka-connector-hubspot==0.1.0a9",
  "sanka-connector-markdown==0.1.0a9",
  "sanka-connector-postgres==0.1.0a9",
  "sanka-connector-salesforce==0.1.0a9",
  "sanka-connector-sendgrid==0.1.0a9",
  "sanka-connector-sqlite==0.1.0a9",
  "types-pyyaml>=6",
  "tortoise-orm>=1.1,<2",
  "aiosqlite>=0.20,<1",
  "sqlalchemy>=2.0,<3",
]

[tool.ruff]
line-length = 100
target-version = "py312"
extend-exclude = ["tests/fixtures"]

[tool.ruff.format]
# ruff >= 0.16 formats fenced Python blocks in Markdown by default. Markdown
# here (design docs especially) carries illustrative sketches with
# hand-aligned comments — review artifacts, not formatter-owned code.
exclude = ["*.md"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "C4", "SIM", "RUF"]

[tool.ruff.lint.isort]
known-first-party = [
  "sanka",
  "sanka_connector",
  "sanka_connector_clickhouse",
  "sanka_connector_csv",
  "sanka_connector_hubspot",
  "sanka_connector_markdown",
  "sanka_connector_postgres",
  "sanka_connector_salesforce",
  "sanka_connector_sendgrid",
  "sanka_connector_sqlite",
  "sanka_migrate_mcp",
]

[tool.mypy]
strict = true
python_version = "3.12"
namespace_packages = true
explicit_package_bases = true
exclude = ["tests/fixtures"]
mypy_path = [
  "packages/sanka-migrate/src",
  "packages/sanka-migrate-mcp/src",
]

[tool.pytest.ini_options]
addopts = "-q"
asyncio_mode = "auto"
testpaths = [
  "packages/sanka-migrate/tests",
  "packages/sanka-migrate-mcp/tests",
  "tests",
]
